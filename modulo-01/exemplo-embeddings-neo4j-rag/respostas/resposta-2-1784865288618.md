Com base nas informações fornecidas no contexto, não foi possível encontrar uma explicação detalhada sobre o conceito de "o que é normalização de dados" ou "por que ela é necessária".

O contexto menciona apenas que o processo de normalização deve ser feito após termos as categorias dos usuários, como no exemplo abaixo:

```javascript
const categorias = ["premium", "medium", "basico"];
```

No entanto, o texto não define o mecanismo técnico da normalização nem justifica sua importância para o funcionamento do modelo de machine learning.